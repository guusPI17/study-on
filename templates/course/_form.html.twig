{{ form_start(form) }}
<div class="container">
    <div class="row">
        <div class="col">
            {{ form_widget(form) }}
        </div>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col">
            <button class="btn btn-info">{{ button_label }}</button>
        </div>
    </div>
</div>
{{ form_end(form) }}
