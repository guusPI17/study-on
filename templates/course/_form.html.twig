{{ form_start(form) }}
<div class="container">
    <div class="row">
        <div class="col"></div>
        <div class="col-12">
            {{ form_widget(form) }}
        </div>
        <div class="col"></div>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col">
            <button class="btn btn-info">{{ button_label|default('Сохранить') }}</button>
        </div>
    </div>
</div>
{{ form_end(form) }}
